fn main() {
    println!("Hello, world!");
}

enum Instruction {
    
}

/// Resgisters are the memory units in the CPU that temporarily store data and instructions and address.
enum Register {
    R_R0,
    R_R1,
    R_R2,
    R_R3,
    R_R4,
    R_R5,
    R_R6,
    R_R7,
    R_PC,
    R_COND,
    R_COUNT,
} 